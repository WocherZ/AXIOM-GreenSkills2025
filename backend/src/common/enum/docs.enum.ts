export enum TextMode {
  GENERATE = 'generate',
  EDIT = 'edit',
}

export enum EditorMode {
  STRUCTURED = 'structured',
  FREEFORM = 'freeform',
}

export enum DocGenerateStatus {
  DRAFT = 'draft',
  GENERATE = 'generate',
}

export enum TextAmount {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg',
}

export enum FontSize {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg',
}

export enum ImageProvider {
  AI_GENERATED = 'aiGenerated',
  USER_UPLOADED = 'userUploaded',
}

export enum StyleTemplate {
  DEFAULT = 'deck_default',
  CUSTOM = 'custom',
}

export enum VerticalAlign {
  TOP = 'top',
  CENTER = 'center',
  BOTTOM = 'bottom',
}

export enum CardDimensions {
  FLUID = 'fluid',
  FIXED = 'fixed',
}

export enum DefaultFullBleed {
  CONTAINED = 'contained',
  FULL = 'full',
}
